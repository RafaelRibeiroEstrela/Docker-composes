version: '3.8'
services:
  redis:
    container_name: redis-container
    image: redis
    ports:
      - '6379:6379'
    command: redis-server
    volumes: 
      - store:/data
    networks:
      - rede1    

networks:
  rede1:
    external: true

volumes:
  store:
    driver: local     
